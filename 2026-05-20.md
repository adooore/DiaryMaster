# DiaryMaster 记忆系统（Hermes 风格）— 开发任务表

**日期**：2026-05-20  
**用途**：多会话并行开发；每完成一条，在本文件把状态改为 `已完成`，并写上完成日期（可选备注）。

**状态标记**：`待办` | `进行中` | `已完成` | `跳过`

---

## 背景（一句话）

在不动笔记工作区的前提下，增加「跨会话长期记忆（双文件 + memory 工具）」与「跨 Session 历史检索」，对齐 Hermes 主机制；笔记正文仍走 `read_file` / 工作区。

**边界**：记忆只存用户偏好、习惯、纠正、工作区元约定；不存笔记全文、不存当前任务进度。

**默认技术选择**（未另行约定则按此实现）：

- 路径：`data/memories/USER.md`、`data/memories/MEMORY.md`（随 `data/` gitignore）
- 字符上限：USER 1375、MEMORY 2200（与 Hermes 同量级，可配置常量）
- **Session 级冻结快照**：本会话启动时读入 system prompt，会话内 `memory` 写盘但不改当轮 prompt；下一会话生效
- 条目分隔：`§`

---

## 任务分布总览


| 阶段   | 任务 ID | 一句话                              | 状态  |
| ---- | ----- | -------------------------------- | --- |
| P0-1 | M1    | 记忆文件存储层 `memory_store.py`        | 已完成  |
| P0-2 | M2    | 组装 system prompt（含占用率块）          | 已完成  |
| P0-3 | M3    | 注册 `memory` 工具 + 写入策略 prompt     | 已完成  |
| P0-4 | M4    | Agent 接入：快照缓存 + context_usage    | 已完成  |
| P1-1 | M5    | 跨 Session 检索 `search_past_chats` | 已完成  |
| P1-2 | M6    | 设置页或 API：查看/编辑记忆文件（可选）           | 已完成  |
| P2   | M7+   | 压缩前 flush、FTS、外部 Provider（后置）    | 待办  |


**建议开会话顺序**：M1 → M2 → M3 → M4（P0 闭环）→ M5 → M6。M2/M3 可并行前需 M1 完成。

---

## P0 — 长期记忆（双文件 + memory 工具）

### M1 — `memory_store.py` 存储层


| 字段       | 内容                        |
| -------- | ------------------------- |
| **状态**   | 已完成                        |
| **完成日**  | 2026-05-20                |
| **主要产出** | `backend/memory/store.py` |
| **依赖**   | 无                         |


**需求**

- 常量：`MEMORIES_DIR`、`USER_LIMIT`、`MEMORY_LIMIT`
- 读写 `USER.md` / `MEMORY.md`；不存在则空文件
- 条目解析/序列化：以 `§` 分隔；对外 `list_entries(store)`、`format_for_prompt(store)`
- `usage(store) -> {used, limit, percent}` 供 prompt 显示
- `add(store, text)`：去重、超限抛错
- `replace(store, old_text, new_text)`：子串唯一匹配
- `remove(store, old_text)`：子串唯一匹配
- 模块与函数中文 docstring

**验收**

- 单元测试或 `python -c` 可手动验证 add/replace/remove/超限

---

### M2 — System prompt 注入记忆块


| 字段       | 内容                                                   |
| -------- | ---------------------------------------------------- |
| **状态**   | 已完成                                                   |
| **完成日**  | 2026-05-20                                              |
| **主要产出** | `backend/memory/prompt.py` |
| **依赖**   | M1                                                   |


**需求**

- `build_system_prompt(base: str, memory_snapshot: str | None) -> str`
- 记忆块格式含标题、`[67% — used/limit chars]`、USER / MEMORY 两段正文
- **快照参数**：由调用方传入字符串，不在此函数内读盘（便于 Session 冻结）
- `SYSTEM_PROMPT` 规则正文保留；记忆块拼在规则后或前（固定一种，写进注释）

**验收**

- 给定 snapshot 字符串，输出稳定、同输入同输出（利于缓存思路）

---

### M3 — `memory` 工具 + 写入策略


| 字段       | 内容                                                       |
| -------- | -------------------------------------------------------- |
| **状态**   | 已完成                                                       |
| **完成日**  | 2026-05-20                                                          |
| **主要产出** | `agent.py` 中 `@tool memory`；`SYSTEM_PROMPT` 增补「记什么/不记什么」 |
| **依赖**   | M1                                                       |


**需求**

- 工具参数：`action: add|replace|remove`，`store: user|memory`，及 `text` / `old_text` / `new_text`
- 调用 `memory_store`，返回简短中文结果（成功 / 失败原因）
- System prompt 明确：
  - 应记：偏好、纠正、长期习惯、工作区整理约定（非正文）
  - 不记：笔记全文、当前任务进度、临时 TODO
  - 超过约 80% 占用时先 consolidate 再 add
- 工具注册进 `_build_agent` 的 `tools` 列表

**验收**

- Agent 在对话中可调用；写盘后 `data/memories/*.md` 内容变化

---

### M4 — Agent 接入：Session 快照 + token 估算


| 字段       | 内容                                                               |
| -------- | ---------------------------------------------------------------- |
| **状态**   | 已完成                                                               |
| **完成日**  | 2026-05-20                                                         |
| **主要产出** | 改 `agent.py` `_build_agent` / `chat_stream`；改 `context_usage.py` |
| **依赖**   | M1、M2、M3                                                         |


**需求**

- **Session 级快照**：`new_session` / `switch_session` / 应用启动后首次 `chat_stream` 时，从磁盘读取记忆并缓存（如 `session._memory_snapshot` 或模块级 `dict[session_id, str]`）
- `_get_agent` 或每轮调用使用 **该 Session 的快照** 构建 `system_prompt`（本会话内不随 memory 工具更新而变）
- `memory` 工具仍立即写盘（下一会话 / 新 Session 可见）
- `context_usage._system_prompt_tokens()` 改为使用「带记忆块的 effective prompt」或接受估算参数
- P0 不要求 UI

**验收**

- 同 Session 内 memory add 后，下一轮对话行为仍基于旧快照；新开 Session 后行为反映新记忆
- `/api/session/context-usage` 占用随记忆变长而上升

---

## P1 — 跨 Session 检索 + 可选 UI

### M5 — `search_past_chats` 工具


| 字段       | 内容                                            |
| -------- | --------------------------------------------- |
| **状态**   | 已完成                                            |
| **完成日**  | 2026-05-20                                               |
| **主要产出** | `backend/memory/search.py` + `agent.py` 工具注册 |
| **依赖**   | P0 完成（至少 M1；与 M4 无强耦合）                        |


**需求**

- 遍历 `data/sessions/*.json`，搜 `chat_log`（优先）或 `messages` 文本
- 第一版：关键词 / 子串匹配即可，无需 SQLite
- 工具：`query`，`limit`（默认 5）；返回 session_id、标题、时间、摘要片段
- 排除当前 session 或标注 `is_current`；只读，不自动 `switch_session`
- System prompt 一句：何时用检索 vs 何时用 memory / read_file

**验收**

- 用户问「之前哪次说过 X」，Agent 能搜到历史 session 片段

---

### M6 — 查看/编辑记忆（可选）


| 字段       | 内容                                 |
| -------- | ---------------------------------- |
| **状态**   | 已完成                                 |
| **完成日**  | 2026-05-20                                    |
| **主要产出** | `GET/PUT /api/memories` + `backend/memory/api.py` + 设置页 |
| **依赖**   | M1                                 |


**需求**

- API 返回 USER / MEMORY 原文 + usage
- PUT 允许用户手工改正错误记忆（校验字符上限）
- 前端：设置区两个文本框或「打开记忆文件」说明

**验收**

- [x] 浏览器可查看并保存，Agent 下一 Session 快照包含新内容

**备注**：时间紧可标 `跳过`，不影响 P0 闭环。

---

## P2 — 后置（本次表不展开实现）


| 任务 ID | 说明                         | 状态  |
| ----- | -------------------------- | --- |
| M7    | 上下文压缩前 `flush` memory 到磁盘  | 进行中  |
| M8    | SQLite FTS 或语义检索替代简单子串     | 待办  |
| M9    | 外部 memory Provider（如 Mem0） | 待办  |
| M10   | `data/skills/*.md` 可选注入    | 待办  |


---

## 多会话协作说明

1. 开一个 Cursor 会话，认领 **一个** 任务 ID（如「只做 M1」）。
2. 把上表对应行的 **状态** 改为 `进行中`（可选在备注写会话/分支名）。
3. 完成后改为 `已完成`，填 **完成日**；勾选该任务下所有 `- [ ]` 为 `- [x]`。
4. 勿在同一任务上并行两个会话，避免冲突。
5. 若实现与表内「默认技术选择」不一致，在任务备注里写一句原因。

---

## 变更记录


| 日期         | 说明                             |
| ---------- | ------------------------------ |
| 2026-05-20 | 初版任务表（P0 M1–M4，P1 M5–M6，P2 占位） |


